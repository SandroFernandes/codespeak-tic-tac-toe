services:
  tic-tac-toe:
    build: .
    ports:
      - "8080:80"    # HTTP — accessible at http://localhost:8080
      - "443:443"    # HTTPS — accessible at https://test.pwa
    restart: unless-stopped
